<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VALORANT</title>
    <link rel="icon" type="image/x-icon" href="/assets/favicon.jpg">
        <style>
        @keyframes slideInLeft {
            0% {
                transform: translateY(200%);
            }

            100% {
                transform: translateY(0);
            }
        }
        @keyframes slideInRight {
            0% {
                transform: translateX(-50%);
            }

            100% {
                transform: translateY(0);
            }
        }

        .log {
            background-image: url(../assets/loginbg.jpg);
            background-size: cover;
            background-attachment: fixed;
            background-position: center;
            position: absolute;
            height: 100vh;
            width: 100%;
            margin-top: -10px;
            width: 100%;
            left: 0;
            right: 0;
            overflow: hidden;

            .wrapper {
                --input-focus: #2d8cf0;
                --font-color: #323232;
                --font-color-sub: #666;
                --bg-color: #fff;
                --bg-color-alt: #666;
                --main-color: #323232;
                /* display: flex; */
                /* flex-direction: column; */
                /* align-items: center; */
            }

            .wrapper {
                animation: 3s ease-out 0s 1 slideInLeft;
                position: relative;
                top: 7%;
                left: 70%;
            }

            /* switch card */
            .switch {
                transform: translateY(-200px);
                position: relative;
                display: flex;
                flex-direction: column;
                justify-content: center;
                align-items: center;
                gap: 30px;
                width: 50px;
                height: 20px;
            }

            .card-side::before {
                position: absolute;
                content: 'Log in';
                left: -84px;
                top: -8px;
                width: 100px;
                color: #ff4655;
                text-transform: uppercase;
                font-size: 20px;
                white-space: nowrap;
                font-family: Arial, Helvetica, sans-serif;
                font-weight: 800;
            }

            .card-side::after {
                position: absolute;
                content: 'Sign up';
                left: 60px;
                top: -8px;
                width: 100px;
                color: #ff4655;
                text-transform: uppercase;
                font-size: 20px;
                white-space: nowrap;
                font-family: Arial, Helvetica, sans-serif;
                font-weight: 800;
            }

            .toggle {
                opacity: 0;
                width: 0;
                height: 0;
            }

            .slider {
                box-sizing: border-box;
                border-radius: 5px;
                border: 2px solid #ff4655;
                box-shadow: 4px 4px #ff4655;
                position: absolute;
                cursor: pointer;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background-color: var(--bg-colorcolor);
                transition: 0.3s;
            }

            .slider:before {
                box-sizing: border-box;
                position: absolute;
                content: "";
                height: 20px;
                width: 20px;
                border: 2px solid #ff4655;
                border-radius: 5px;
                left: -2px;
                bottom: 2px;
                background-color: #ff4655;
                box-shadow: 0 3px 0 var(--main-color);
                transition: 0.3s;
            }

            .toggle:checked+.slider {
                background-color: var(--bg-colorcolor);
            }

            .toggle:checked+.slider:before {
                transform: translateX(30px);
            }

            .toggle:checked~.card-side:before {
                text-decoration: none;
            }

            .toggle:checked~.card-side:after {
                text-decoration: none;
            }

            /* card */

            .flip-card__inner {
                width: 300px;
                height: 350px;
                position: relative;
                background-color: transparent;
                perspective: 1000px;
                /* width: 100%;
    height: 100%; */
                text-align: center;
                transition: transform 0.8s;
                transform-style: preserve-3d;
            }

            .toggle:checked~.flip-card__inner {
                transform: rotateY(180deg);
            }

            .toggle:checked~.flip-card__front {
                box-shadow: none;
            }

            .flip-card__front,
            .flip-card__back {
                padding: 20px;
                position: absolute;
                display: flex;
                flex-direction: column;
                justify-content: center;
                -webkit-backface-visibility: hidden;
                backface-visibility: hidden;
                background: rgb(3, 30, 47);
                gap: 20px;
                border-radius: 5px;
                box-shadow: 4px 4px #ff4655;
            }

            .flip-card__back {
                width: 100%;
                transform: rotateY(180deg);
            }

            .flip-card__form {
                display: flex;
                flex-direction: column;
                align-items: center;
                gap: 20px;
            }

            .title {
                margin: 20px 0 20px 0;
                font-size: 25px;
                font-weight: 900;
                text-align: center;
                color: white;
                text-transform: uppercase;
                font-size: 25px;
                white-space: nowrap;
                font-family: freight big pro;
                font-weight: 100;
            }

            .flip-card__input {
                width: 250px;
                height: 40px;
                border-radius: 5px;
                border: 2px solid rgb(1, 43, 68);
                background-color: var(--bg-color);
                box-shadow: 4px 4px rgb(1, 43, 68);
                font-size: 15px;
                font-weight: 100;
                color: var(--font-color);
                font-family: feight big pro;
                padding: 5px 10px;
                outline: none;
            }

            .flip-card__input::placeholder {
                color: var(--font-color-sub);
                opacity: 0.8;
            }

            .flip-card__input:focus {
                border: 2px solid #ff4655;
            }

            .flip-card__btn:active,
            .button-confirm:active {
                box-shadow: 0px 0px var(--main-color);
                transform: translate(3px, 3px);
            }

            .flip-card__btn {
                margin: 20px 0 20px 0;
                width: 120px;
                height: 40px;
                border-radius: 5px;
                border: 2px solid rgb(1, 43, 68);
                background-color: var(--bg-color);
                box-shadow: 4px 4px rgb(1, 43, 68);
                font-size: 17px;
                font-weight: 600;
                color: var(--font-color);
                cursor: pointer;
            }

            .flip-card__btn:hover {
                background-color: #ff4655;
                color: white;
            }

            .data {
                position: relative;
                top: 15%;
                left: 5%;
            }

            .data p {
                animation: 2s ease-out 0s 1 slideInRight;
                color: rgb(255, 255, 255);
                font-stretch: ultra-expanded;
                font-size: 100px;
                white-space: nowrap;
                font-family: freight big pro;
                font-weight: 300;
                letter-spacing: -0.2rem;
                margin-top: -30px;
            }
        }
    </style>
</head>

<body>
    <section class="log">
        <div class="data">
            <p>Get yo <br><span style="color:#ff4655;"><em>setup</em></span> READY</p>
        </div>
        <div class="wrapper">
            <div class="card-switch">
                <label class="switch">
                    <input class="toggle" type="checkbox">
                    <span class="slider"></span>
                    <span class="card-side"></span>
                    <div class="flip-card__inner">
                        <div class="flip-card__front">
                            <div class="title">Log in</div>
                            <form action="/login" method="post" class="flip-card__form">
                                <input type="text" name="username" placeholder="Username" required class="flip-card__input">
                                <input type="password" name="password" placeholder="Password" required class="flip-card__input">
                                <button class="flip-card__btn">Let`s go!</button>
                            </form>
                        </div>
                        <div class="flip-card__back">
                            <div class="title">Sign up</div>
                            <form action="/register" method="post" class="flip-card__form">
                                
                                <input type="text" name="username" placeholder="Username" required class="flip-card__input">
                                <input type="email" name="email" placeholder="Email" required class="flip-card__input">
                                <input type="password" name="password" placeholder="Password" required class="flip-card__input">
                                <button class="flip-card__btn">Confirm!</button>
                            </form>
                        </div>
                    </div>
                </label>
            </div>
        </div>
    </section>
</body>

</html>